{
  "name": "standardoc-cli",
  "version": "0.1.0",
  "description": "CLI pour parser et générer de la documentation depuis les commentaires avec @tags",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "standardoc": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc && node scripts/add-extensions.js",
    "watch": "tsc --watch",
    "test": "bun test",
    "dev": "bun run build && node dist/cli/cli.js"
  },
  "keywords": [
    "documentation",
    "comments",
    "parser",
    "tags",
    "docs",
    "cli"
  ],
  "author": "Standardoc",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "fast-glob": "^3.3.1"
  }
}

